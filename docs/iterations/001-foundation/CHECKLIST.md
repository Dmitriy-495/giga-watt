# Iteration 001 — Foundation Checklist

## Цель

Создать первый рабочий слой цифрового двойника предприятия: организационную
структуру, сотрудников, штатную структуру и объекты эксплуатации.

---

## Организационная структура

- [x] Фиксированная иерархия Учреждение → Филиал → ЖКС → ПУ (`parent_id` +
      триггер `validate_organization_unit_hierarchy`)
- [x] Обязательные реквизиты (наименование, местоположение, адрес,
      координаты, телефон, e-mail, руководитель)
- [x] Backend API: `organization-units` (List/Get/Create/Update)

---

## Сотрудники и штатная структура

- [x] Справочник должностей (`positions`, CRUD)
- [x] Справочник сотрудников (`employees`, CRUD)
- [x] Автогенерация сокращённого ФИО из фамилии и инициалов
- [x] Телефоны и e-mail сотрудников (1:N, не более одного основного —
      частичные уникальные индексы в БД)
- [x] Кадровые назначения (`employee_assignments`), поддержка нескольких
      назначений одного сотрудника (история + текущее)
- [x] Базовая модель штатных единиц (`staff_positions`)

---

## Объекты эксплуатации

- [x] Справочник типов объектов (`object_types`)
- [x] Справочник назначений объектов (`object_purposes`), назначение
      связано с типом объекта (составной FK + фильтр в API)
- [x] Сущность объекта эксплуатации (`operational_objects`)
- [x] Объект эксплуатации может принадлежать только ПУ (триггер
      `validate_operational_object_owner`)

---

## Backend

- [x] Модули: `organization`, `position`, `employee`, `staffposition`,
      `objecttype`, `objectpurpose`, `operationalobject`
- [x] SQL-first, pgx без ORM (ADR-0001)
- [x] Предметные ошибки (нарушение ограничений БД) возвращаются клиенту
      как понятные сообщения (422), а не сырые ошибки Postgres
- [x] `go build`, `go vet`, `gofmt` — чисто

---

## Frontend

- [x] Страницы списков для всех сущностей Foundation (организации,
      должности, сотрудники, штатные единицы, типы/назначения объектов,
      объекты эксплуатации)

---

## Database

- [x] Миграции 000001–000005 применяются на чистой БД без ошибок
- [x] Ключевые бизнес-правила защищены триггерами/ограничениями БД, а не
      только в backend

---

## Completion

- [x] CHECKPOINT_001-001 published

---

## Foundation Hardening + Fixtures (технический проход)

- [x] Найден и исправлен дефект триггера иерархии: смена `type` у узла с
      существующими детьми могла оставить их под недопустимым родителем
      или образовать цикл в `parent_id` (миграция `000006`)
- [x] Regression-тесты (`go test` против реальной PostgreSQL) для
      инвариантов: иерархия (валидные/невалидные уровни, корневой
      уровень, смена типа с детьми, репаринтинг без смены типа, циклы,
      self-parent), кадровые назначения, контакты (primary),
      штатные единицы, объекты эксплуатации (владение ПУ, type/purpose)
- [x] `organization_units` намеренно без `DELETE` — задокументировано
      (NOTES.md Decision Log + комментарий в коде), не добавлено
      автоматически
- [x] Исправлено противоречие в `ITERATION.md` (должность как
      "обязательный реквизит" сотрудника)
- [x] `CHANGELOG.md`, `docs/DEVELOPMENT.md`, `docs/VERSION.md` приведены
      в соответствие фактическому состоянию (включая frontend-стек)
- [x] ADR-0005 (схема версионирования)
- [x] Excel fixtures (`fixtures/initial-data.xlsx`) + loader
      (`backend/cmd/fixtures`, excelize, транзакционно, с валидацией)
- [x] Полный воспроизводимый цикл (reset → migrations → fixtures →
      backend → frontend → тесты) проверен дважды с идентичным
      результатом

### Закрытие технического долга (по решению пользователя)

- [x] Down-миграции для всех 6 миграций; проверен полный и частичный
      up/down/up цикл (включая откат `000006` к поведению `000003`)
- [x] Regression-тест на explicit cycle-check в изоляции от
      children-block (`TestHierarchy_CycleWalkCatchesPreexistingCycle`)
- [x] CI (`.github/workflows/ci.yml`): build/vet/gofmt + Postgres +
      миграции + тесты + fixtures + frontend build
- [x] Обнаружено и задокументировано (ADR-0004, addendum): фактическое
      минимальное требование — Go 1.25, а не 1.22.3 (предсуществующая
      транзитивная зависимость `cleanenv → yaml.v3 → kr/pretty →
      go-internal`, не связано с добавлением excelize)
- [ ] `organization_units` DELETE — сознательно НЕ добавлен (требует
      отдельного предметного решения о судьбе зависимых записей)
- [ ] Frontend CRUD-формы — сознательно НЕ реализованы (требует
      отдельного обсуждения стека/объёма)

Подробности — `CHECKPOINT_001-002.md`.

---

## Текущий статус

**DONE**

Все критерии завершения Iteration 001 из `ITERATION.md` выполнены, и
проведён технический проход Foundation Hardening + Fixtures (см. выше).

## Следующий этап

Iteration 002 (см. `docs/iterations/002-*/` после согласования темы
итерации).

## Правило завершения

Последним действием Iteration 001 является публикация
`CHECKPOINT_001-001.md`, а технического прохода Hardening —
`CHECKPOINT_001-002.md`.
